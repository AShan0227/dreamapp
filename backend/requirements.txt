fastapi>=0.115.0
uvicorn[standard]>=0.34.0
sqlalchemy>=2.0
aiosqlite>=0.20.0
asyncpg>=0.30.0
# psycopg2-binary: SYNC driver loaded by alembic env.py for migrations.
# App code uses asyncpg (above) for runtime; both coexist cleanly. The
# Docker image bakes this in but pip-only installs (CI, local dev) need it.
psycopg2-binary>=2.9.9
pydantic>=2.0
pydantic-settings>=2.0
httpx>=0.28.0
python-multipart>=0.0.18
PyJWT>=2.8.0
pgvector>=0.3.0
sentence-transformers>=3.0.0
minio>=7.2.0
bcrypt>=4.2.0
alembic>=1.13.0
email-validator>=2.2.0
sentry-sdk[fastapi]>=2.16.0
