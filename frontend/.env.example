# Frontend env vars. Copy to .env or .env.production and edit.
# Vite injects any variable starting with VITE_ into client bundles.

# Backend API base. Leave empty to use same-origin (production behind nginx).
VITE_API_BASE_URL=
